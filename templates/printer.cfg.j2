# This file was generated by KACE (Klipper Automated Configuration Ecosystem)
# Board: {{ user.board }}
# Kinematics: {{ user.kinematics }}

[include mainsail.cfg]

[mcu]
# Serial connection to the printer controller board.
# This value is automatically detected by KACE.
# If the printer cannot connect, verify the path in /dev/serial/by-id/.
serial: {{ user.mcu_path }}

[printer]
# Printer kinematics type.
# Common values:
# cartesian
# corexy
# delta
kinematics: {{ user.kinematics }}
# Maximum movement speeds and accelerations.
max_velocity: 300
max_accel: 3000
max_z_velocity: 5
max_z_accel: 100

[stepper_x]
{%- if 'stepper_x' in pins %}
# Step pin for the X stepper driver.
step_pin: {{ pins['stepper_x'].get('step_pin', 'TODO') }}
# Direction pin.
# If the motor moves in the wrong direction,
# add or remove "!" before the pin.
dir_pin: {{ pins['stepper_x'].get('dir_pin', 'TODO') }}
# Enable pin for the stepper driver.
enable_pin: {{ pins['stepper_x'].get('enable_pin', 'TODO') }}
{%- endif %}
microsteps: 16
rotation_distance: 40
# Endstop pin.
# If the endstop logic is reversed,
# add or remove "!" before the pin.
endstop_pin: {{ pins['stepper_x'].get('endstop_pin', 'TODO') if 'stepper_x' in pins else 'TODO' }}
position_endstop: 0
position_max: {{ user.x_size }}
homing_speed: 50
{% if user.driver_type != 'None (Standard)' and user.driver_type != 'A4988' and user.driver_mode != 'Standalone' %}
{%- set tmc = pins.get('tmc2209 stepper_x', pins.get('tmc2208 stepper_x', pins.get('tmc2130 stepper_x', pins.get('tmc5160 stepper_x', {})))) %}
{%- set has_pins = (user.driver_mode == 'UART' and 'uart_pin' in tmc) or (user.driver_mode == 'SPI' and 'cs_pin' in tmc) %}
{%- set prefix = '' if has_pins else '# ' %}
{{ prefix }}[{{ user.driver_type | lower }} stepper_x]
{%- if user.driver_mode == 'UART' %}
{{ prefix }}# UART communication pin
{{ prefix }}uart_pin: {{ tmc.get('uart_pin', 'TODO') }}
{%- if 'tx_pin' in tmc %}
{{ prefix }}tx_pin: {{ tmc.get('tx_pin') }}
{%- endif %}
{%- elif user.driver_mode == 'SPI' %}
{{ prefix }}cs_pin: {{ tmc.get('cs_pin', 'TODO') }}
{%- if 'spi_software_sclk_pin' in tmc %}
{{ prefix }}spi_software_sclk_pin: {{ tmc.get('spi_software_sclk_pin') }}
{%- endif %}
{%- if 'spi_software_mosi_pin' in tmc %}
{{ prefix }}spi_software_mosi_pin: {{ tmc.get('spi_software_mosi_pin') }}
{%- endif %}
{%- if 'spi_software_miso_pin' in tmc %}
{{ prefix }}spi_software_miso_pin: {{ tmc.get('spi_software_miso_pin') }}
{%- endif %}
{%- if 'spi_bus' in tmc %}
{{ prefix }}spi_bus: {{ tmc.get('spi_bus') }}
{%- endif %}
{%- endif %}
{{ prefix }}run_current: 0.800
{{ prefix }}hold_current: 0.400
{{ prefix }}# If stealthchop_threshold = 0
{{ prefix }}# the driver will use spreadCycle mode.
{{ prefix }}stealthchop_threshold: 999999
{%- endif %}

[stepper_y]
{%- if 'stepper_y' in pins %}
# Step pin for the Y stepper driver.
step_pin: {{ pins['stepper_y'].get('step_pin', 'TODO') }}
# Direction pin.
# If the motor moves in the wrong direction,
# add or remove "!" before the pin.
dir_pin: {{ pins['stepper_y'].get('dir_pin', 'TODO') }}
# Enable pin for the stepper driver.
enable_pin: {{ pins['stepper_y'].get('enable_pin', 'TODO') }}
{%- endif %}
microsteps: 16
rotation_distance: 40
# Endstop pin.
# If the endstop logic is reversed,
# add or remove "!" before the pin.
endstop_pin: {{ pins['stepper_y'].get('endstop_pin', 'TODO') if 'stepper_y' in pins else 'TODO' }}
position_endstop: 0
position_max: {{ user.y_size }}
homing_speed: 50
{% if user.driver_type != 'None (Standard)' and user.driver_type != 'A4988' and user.driver_mode != 'Standalone' %}
{%- set tmc = pins.get('tmc2209 stepper_y', pins.get('tmc2208 stepper_y', pins.get('tmc2130 stepper_y', pins.get('tmc5160 stepper_y', {})))) %}
{%- set has_pins = (user.driver_mode == 'UART' and 'uart_pin' in tmc) or (user.driver_mode == 'SPI' and 'cs_pin' in tmc) %}
{%- set prefix = '' if has_pins else '# ' %}
{{ prefix }}[{{ user.driver_type | lower }} stepper_y]
{%- if user.driver_mode == 'UART' %}
{{ prefix }}# UART communication pin
{{ prefix }}uart_pin: {{ tmc.get('uart_pin', 'TODO') }}
{%- if 'tx_pin' in tmc %}
{{ prefix }}tx_pin: {{ tmc.get('tx_pin') }}
{%- endif %}
{%- elif user.driver_mode == 'SPI' %}
{{ prefix }}cs_pin: {{ tmc.get('cs_pin', 'TODO') }}
{%- if 'spi_software_sclk_pin' in tmc %}
{{ prefix }}spi_software_sclk_pin: {{ tmc.get('spi_software_sclk_pin') }}
{%- endif %}
{%- if 'spi_software_mosi_pin' in tmc %}
{{ prefix }}spi_software_mosi_pin: {{ tmc.get('spi_software_mosi_pin') }}
{%- endif %}
{%- if 'spi_software_miso_pin' in tmc %}
{{ prefix }}spi_software_miso_pin: {{ tmc.get('spi_software_miso_pin') }}
{%- endif %}
{%- if 'spi_bus' in tmc %}
{{ prefix }}spi_bus: {{ tmc.get('spi_bus') }}
{%- endif %}
{%- endif %}
{{ prefix }}run_current: 0.800
{{ prefix }}hold_current: 0.400
{{ prefix }}# If stealthchop_threshold = 0
{{ prefix }}# the driver will use spreadCycle mode.
{{ prefix }}stealthchop_threshold: 999999
{%- endif %}

[stepper_z]
{%- if 'stepper_z' in pins %}
# Step pin for the Z stepper driver.
step_pin: {{ pins['stepper_z'].get('step_pin', 'TODO') }}
# Direction pin.
# If the motor moves in the wrong direction,
# add or remove "!" before the pin.
dir_pin: {{ pins['stepper_z'].get('dir_pin', 'TODO') }}
# Enable pin for the stepper driver.
enable_pin: {{ pins['stepper_z'].get('enable_pin', 'TODO') }}
{%- endif %}
microsteps: 16
rotation_distance: 8
{%- if user.probe != 'None' %}
# Endstop pin.
# If the endstop logic is reversed,
# add or remove "!" before the pin.
endstop_pin: probe:z_virtual_endstop
# endstop_pin: {{ pins['stepper_z'].get('endstop_pin', 'TODO') if 'stepper_z' in pins else 'TODO' }}
# position_endstop: 0
{%- else %}
# Endstop pin.
# If the endstop logic is reversed,
# add or remove "!" before the pin.
endstop_pin: {{ pins['stepper_z'].get('endstop_pin', 'TODO') if 'stepper_z' in pins else 'TODO' }}
position_endstop: 0
{%- endif %}
position_max: {{ user.z_size }}
{% if user.driver_type != 'None (Standard)' and user.driver_type != 'A4988' and user.driver_mode != 'Standalone' %}
{%- set tmc = pins.get('tmc2209 stepper_z', pins.get('tmc2208 stepper_z', pins.get('tmc2130 stepper_z', pins.get('tmc5160 stepper_z', {})))) %}
{%- set has_pins = (user.driver_mode == 'UART' and 'uart_pin' in tmc) or (user.driver_mode == 'SPI' and 'cs_pin' in tmc) %}
{%- set prefix = '' if has_pins else '# ' %}
{{ prefix }}[{{ user.driver_type | lower }} stepper_z]
{%- if user.driver_mode == 'UART' %}
{{ prefix }}# UART communication pin
{{ prefix }}uart_pin: {{ tmc.get('uart_pin', 'TODO') }}
{%- if 'tx_pin' in tmc %}
{{ prefix }}tx_pin: {{ tmc.get('tx_pin') }}
{%- endif %}
{%- elif user.driver_mode == 'SPI' %}
{{ prefix }}cs_pin: {{ tmc.get('cs_pin', 'TODO') }}
{%- if 'spi_software_sclk_pin' in tmc %}
{{ prefix }}spi_software_sclk_pin: {{ tmc.get('spi_software_sclk_pin') }}
{%- endif %}
{%- if 'spi_software_mosi_pin' in tmc %}
{{ prefix }}spi_software_mosi_pin: {{ tmc.get('spi_software_mosi_pin') }}
{%- endif %}
{%- if 'spi_software_miso_pin' in tmc %}
{{ prefix }}spi_software_miso_pin: {{ tmc.get('spi_software_miso_pin') }}
{%- endif %}
{%- if 'spi_bus' in tmc %}
{{ prefix }}spi_bus: {{ tmc.get('spi_bus') }}
{%- endif %}
{%- endif %}
{{ prefix }}run_current: 0.650
{{ prefix }}hold_current: 0.400
{{ prefix }}# If stealthchop_threshold = 0
{{ prefix }}# the driver will use spreadCycle mode.
{{ prefix }}stealthchop_threshold: 999999
{% endif %}

{% if user.z_motors == '2' %}

[stepper_z1]
{%- set z1_pins = pins.get('stepper_z1', pins.get('extruder_1', pins.get('extruder1', {}))) %}
{%- if z1_pins %}
# Step pin for the Z1 stepper driver.
step_pin: {{ z1_pins.get('step_pin', 'TODO') }}
# Direction pin.
# If the motor moves in the wrong direction,
# add or remove "!" before the pin.
dir_pin: {{ z1_pins.get('dir_pin', 'TODO') }}
# Enable pin for the stepper driver.
enable_pin: {{ z1_pins.get('enable_pin', 'TODO') }}
{%- else %}
# Step pin for the Z1 stepper driver.
step_pin: TODO
# Direction pin.
# If the motor moves in the wrong direction,
# add or remove "!" before the pin.
dir_pin: TODO
# Enable pin for the stepper driver.
enable_pin: TODO
{%- endif %}
microsteps: 16
rotation_distance: 8
{% if user.driver_type != 'None (Standard)' and user.driver_type != 'A4988' and user.driver_mode != 'Standalone' %}
{%- set tmc_z1 = pins.get('tmc2209 stepper_z1', pins.get('tmc2208 stepper_z1', pins.get('tmc2130 stepper_z1', pins.get('tmc5160 stepper_z1', pins.get('tmc2209 extruder_1', pins.get('tmc2208 extruder_1', pins.get('tmc2130 extruder_1', pins.get('tmc5160 extruder_1', pins.get('tmc2209 extruder1', pins.get('tmc2208 extruder1', pins.get('tmc2130 extruder1', pins.get('tmc5160 extruder1', {})))))))))))) %}
{%- set has_pins_z1 = (user.driver_mode == 'UART' and 'uart_pin' in tmc_z1) or (user.driver_mode == 'SPI' and 'cs_pin' in tmc_z1) %}
{%- set prefix_z1 = '' if has_pins_z1 else '# ' %}
{{ prefix_z1 }}[{{ user.driver_type | lower }} stepper_z1]
{%- if user.driver_mode == 'UART' %}
{{ prefix_z1 }}# UART communication pin
{{ prefix_z1 }}uart_pin: {{ tmc_z1.get('uart_pin', 'TODO') }}
{%- if 'tx_pin' in tmc_z1 %}
{{ prefix_z1 }}tx_pin: {{ tmc_z1.get('tx_pin') }}
{%- endif %}
{%- elif user.driver_mode == 'SPI' %}
{{ prefix_z1 }}cs_pin: {{ tmc_z1.get('cs_pin', 'TODO') }}
{%- if 'spi_software_sclk_pin' in tmc_z1 %}
{{ prefix_z1 }}spi_software_sclk_pin: {{ tmc_z1.get('spi_software_sclk_pin') }}
{%- endif %}
{%- if 'spi_software_mosi_pin' in tmc_z1 %}
{{ prefix_z1 }}spi_software_mosi_pin: {{ tmc_z1.get('spi_software_mosi_pin') }}
{%- endif %}
{%- if 'spi_software_miso_pin' in tmc_z1 %}
{{ prefix_z1 }}spi_software_miso_pin: {{ tmc_z1.get('spi_software_miso_pin') }}
{%- endif %}
{%- if 'spi_bus' in tmc_z1 %}
{{ prefix_z1 }}spi_bus: {{ tmc_z1.get('spi_bus') }}
{%- endif %}
{%- endif %}
{{ prefix_z1 }}run_current: 0.650
{{ prefix_z1 }}hold_current: 0.400
{{ prefix_z1 }}# If stealthchop_threshold = 0
{{ prefix_z1 }}# the driver will use spreadCycle mode.
{{ prefix_z1 }}stealthchop_threshold: 999999
{% endif %}
{% endif %}

{% if user.probe != 'None' %}
[{{ user.probe | lower }}]
{%- set probe_sec = pins.get('bltouch', pins.get('probe', {})) %}
sensor_pin: {{ probe_sec.get('sensor_pin', '^TODO') }}
{%- if user.probe | lower == 'bltouch' or 'control_pin' in probe_sec %}
control_pin: {{ probe_sec.get('control_pin', 'TODO') }}
{%- endif %}
# Offsets relative to the nozzle.
# These must be measured for your specific printer.
x_offset: 0
y_offset: 0
# Z offset should be calibrated using:
# PROBE_CALIBRATE
z_offset: 0

[safe_z_home]
home_xy_position: {{ user.x_size | int / 2 }}, {{ user.y_size | int / 2 }}
speed: 50
z_hop: 10
z_hop_speed: 5
{%- endif %}

[extruder]
{%- if 'extruder' in pins %}
step_pin: {{ pins['extruder'].get('step_pin', 'TODO') }}
dir_pin: {{ pins['extruder'].get('dir_pin', 'TODO') }}
enable_pin: {{ pins['extruder'].get('enable_pin', 'TODO') }}
heater_pin: {{ pins['extruder'].get('heater_pin', 'TODO') }}
sensor_pin: {{ pins['extruder'].get('sensor_pin', 'TODO') }}
{%- endif %}
microsteps: 16
rotation_distance: 33.500
nozzle_diameter: 0.400
filament_diameter: 1.750
sensor_type: EPCOS 100K B57560G104F
control: pid
pid_Kp: 22.2
pid_Ki: 1.08
pid_Kd: 114
min_temp: 0
max_temp: 250
{% if user.driver_type != 'None (Standard)' and user.driver_type != 'A4988' and user.driver_mode != 'Standalone' %}
{%- set tmc = pins.get('tmc2209 extruder', pins.get('tmc2208 extruder', pins.get('tmc2130 extruder', pins.get('tmc5160 extruder', {})))) %}
{%- set has_pins = (user.driver_mode == 'UART' and 'uart_pin' in tmc) or (user.driver_mode == 'SPI' and 'cs_pin' in tmc) %}
{%- set prefix = '' if has_pins else '# ' %}
{{ prefix }}[{{ user.driver_type | lower }} extruder]
{%- if user.driver_mode == 'UART' %}
{{ prefix }}# UART communication pin
{{ prefix }}uart_pin: {{ tmc.get('uart_pin', 'TODO') }}
{%- if 'tx_pin' in tmc %}
{{ prefix }}tx_pin: {{ tmc.get('tx_pin') }}
{%- endif %}
{%- elif user.driver_mode == 'SPI' %}
{{ prefix }}cs_pin: {{ tmc.get('cs_pin', 'TODO') }}
{%- if 'spi_software_sclk_pin' in tmc %}
{{ prefix }}spi_software_sclk_pin: {{ tmc.get('spi_software_sclk_pin') }}
{%- endif %}
{%- if 'spi_software_mosi_pin' in tmc %}
{{ prefix }}spi_software_mosi_pin: {{ tmc.get('spi_software_mosi_pin') }}
{%- endif %}
{%- if 'spi_software_miso_pin' in tmc %}
{{ prefix }}spi_software_miso_pin: {{ tmc.get('spi_software_miso_pin') }}
{%- endif %}
{%- if 'spi_bus' in tmc %}
{{ prefix }}spi_bus: {{ tmc.get('spi_bus') }}
{%- endif %}
{%- endif %}
{{ prefix }}run_current: 0.650
{{ prefix }}hold_current: 0.400
{{ prefix }}# If stealthchop_threshold = 0
{{ prefix }}# the driver will use spreadCycle mode.
{{ prefix }}stealthchop_threshold: 999999
{%- endif %}

[heater_bed]
{%- if 'heater_bed' in pins %}
heater_pin: {{ pins['heater_bed'].get('heater_pin', 'TODO') }}
sensor_pin: {{ pins['heater_bed'].get('sensor_pin', 'TODO') }}
{%- endif %}
sensor_type: EPCOS 100K B57560G104F
control: pid
pid_Kp: 54.027
pid_Ki: 0.770
pid_Kd: 948.182
min_temp: 0
max_temp: 130

